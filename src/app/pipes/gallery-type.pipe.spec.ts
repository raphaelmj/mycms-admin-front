import { GalleryTypePipe } from './gallery-type.pipe';

describe('GalleryTypePipe', () => {
  it('create an instance', () => {
    const pipe = new GalleryTypePipe();
    expect(pipe).toBeTruthy();
  });
});
