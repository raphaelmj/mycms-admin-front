import { ModelNamePipe } from './model-name.pipe';

describe('ModelNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ModelNamePipe();
    expect(pipe).toBeTruthy();
  });
});
